# Render blueprint — backend API (Docker).
# Connect this repo on render.com → New → Blueprint; Render picks this up
# from the repo root automatically.
services:
  - type: web
    name: cardiolens-api
    runtime: docker
    rootDir: ML_Project_7_A/cardiolens/backend
    plan: free
    healthCheckPath: /health
    autoDeploy: true
    