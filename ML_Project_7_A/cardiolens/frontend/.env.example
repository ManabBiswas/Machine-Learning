# Base URL of the deployed FastAPI backend (no trailing slash, no /api suffix).
# Leave unset for local dev — Vite proxies /api to http://localhost:8000.
VITE_API_URL=https://your-backend.onrender.com
